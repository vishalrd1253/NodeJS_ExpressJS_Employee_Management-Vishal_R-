<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome Page</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
            background-color: #f3f6f9;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .box {
            background: #ffffff;
            padding: 40px;
            border-radius: 24px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.05);
            width: 100%;
            max-width: 400px;
            text-align: center;
            border: 1px solid #eef2f7;
        }

        .heading {
            color: #2d3748;
            font-size: 26px;
            font-weight: 800;
            margin-bottom: 30px;
            letter-spacing: -0.5px;
        }

        button {
            display: block;
            width: 100%;
            margin-bottom: 14px;
            border: none;
            border-radius: 12px;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            cursor: pointer;
        }

        button:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
            filter: brightness(1.1);
        }

        button a {
            display: block;
            padding: 15px 20px;
            text-decoration: none;
            color: #ffffff;
            font-weight: 700;
            font-size: 15px;
            letter-spacing: 0.3px;
        }

        .home {
            background-color: #6366f1; 
        }

        .add {
            background-color: #10b981; 
        }

        .view {
            background-color: #3b82f6; 
        }

        .update {
            background-color: #f59e0b; 
        }

        .delete {
            background-color: #ef4444; 
        }

        .logout {
            background-color: #64748b; 
            margin-top: 20px;
        }

        button:active {
            transform: scale(0.97);
        }
    </style>
</head>
<body>
    <div class="box">
        <h1 class="heading"><%=msg%></h1>
        <button class="home"><a href="/home">Home Page</a></button>
        <button class="add"><a href="/add">Add Employee</a></button>
        <button class="view"><a href="/view">View All Employee</a></button>
        <button class="update"><a href="/update">Update Employee</a></button>
        <button class="update"><a href="/view/username">View by Username</a></button>
        <button class="delete"><a href="/delete">Delete Employee</a></button>
        <button class="logout"><a href="/logout">Logout</a></button>
    </div>
</body>
</html>